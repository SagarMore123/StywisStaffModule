<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="dto"
            type="com.astrika.stywis_staff.models.stock.StocksCustomizationDTO" />


    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="7dp"
        android:background="@drawable/circular_rounded_corner_white_background_with_grey_stroke"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="5dp">

        <TextView
            android:id="@+id/option_txt"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/air_bnb_cereal_medium"
            android:gravity="center"
            android:text="@{dto.customizeOptionName + ``}"
            android:textColor="@color/colorBlack"
            tools:text="1 Kg" />

        <View
            android:layout_width="0dp"
            android:layout_height="2dp"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            android:layout_weight="0.2"
            android:background="@color/colorBlack" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10dp"
            android:layout_weight="1"
            android:fontFamily="@font/air_bnb_cereal_medium"
            android:gravity="center"
            android:text="@{dto.presentQuantity + ` Nos.`}"
            android:textColor="@color/colorBlack"
            tools:text="2 Nos." />


        <ImageView
            android:id="@+id/ic_remove"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:backgroundTint="@color/colorBlack"
            android:src="@drawable/ic_close" />

    </LinearLayout>

</layout>